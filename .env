# Postgres connection string. Use Vercel Postgres (Neon) or any managed Postgres.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require
DATABASE_URL=
# Admin password for /admin (keep secret)
ADMIN_SECRET=quice
